import 'package:flutter/material.dart';

const Color backgroundColor = Color(0xFFFBFBFD);
const Color primary = Color(0xFF3189BE);
const Color secondary = Color(0xFFE5322E);
const Color greyColor = Color(0xFFACB3BF);

const String androidVersionNumber = '0.0.1';
const RoundedRectangleBorder snackbarborder = RoundedRectangleBorder(
    borderRadius: BorderRadius.only(
        topLeft: Radius.circular(20), topRight: Radius.circular(20)));

const TextStyle appbarTextStyle = TextStyle(
    fontWeight: FontWeight.bold,
    fontSize: 24,
    color: Colors.white);
